# docker-compose.yml
version: '3'
services:
  backend:
    build: ./backend   
    container_name: backend
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI=mongodb+srv://admin:u4aAj4xkj2QZfoap@cluster0.npomqzy.mongodb.net/Expense-management
      - NODE_ENV=production
      - JWT_SECRET=mytops3cr3t

  frontend:
    build: ./frontend  
    container_name: frontend
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  mongo-data:
